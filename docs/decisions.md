min_length=2 для q и причина (осмысленность поискового запроса для semantic search — отсекает бессмысленные
1-символьные вводы, оставляет валидные короткие аббревиатуры).

Буду ловить через except redis.exceptions.ConnectionError как он специально заточен ловить именно незапланированную
остановку сервиса

Решение: поле cache_status в логах search ()

Поле: cache_status
Значения: HIT | MISS | READ_FAILED | WRITE_FAILED
Почему разделены read/write: В логах можно будет разделять и понимать на каком этапе произошла ошибка поможет при
отладке

Пробую записывать кэш ещё раз после того как получаю данные из базы данных set ()

буду патчить фабричную функцию + сбрасывать кеш перед каждым тестом потому что если делать так с клиентом то некоторые тесты могут влиять друг на друга

Ошибка в тесте на Redis unavailable: `any(record.cache_status == ...)` падал
с AttributeError на записях без этого поля (profiler, httpx2), потому что
any() вычисляет условие для каждой записи по очереди и не пропускает
исключения. Решение: `getattr(record, "cache_status", None)` — безопасно
возвращает None для записей без атрибута, сравнение становится False
вместо падения.